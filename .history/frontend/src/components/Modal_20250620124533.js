import React from 'react'

const Modal = ({children, isClose, onClose, title}) => {
  return (
    <div>
      <div>
        {/* Modal content */}
        <div>
            {Modal }
        </div>
      </div>
    </div>
  )
}

export default Modal
