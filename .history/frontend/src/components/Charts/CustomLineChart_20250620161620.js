import React from 'react';
import { XAxis, YAxis, Tooltip, Res } from 'recharts';

const CustomLineChart = ({ data }) => {
  return (
    <div>
      
    </div>
  )
}

export default CustomLineChart
