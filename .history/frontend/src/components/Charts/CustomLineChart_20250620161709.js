import React from 'react';
import { XAxis, YAxis, Tooltip, ResponsiveContainer, CartesianGrid, Area } from 'recharts';

const CustomLineChart = ({ data }) => {
  return (
    <div>
      
    </div>
  )
}

export default CustomLineChart
