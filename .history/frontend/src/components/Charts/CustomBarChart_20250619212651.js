import React from 'react';
import { BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, Legend, Responsive } from 'recharts';
const CustomBarChart = ({data}) => {
  return (
    <div>
      
    </div>
  )
}

export default CustomBarChart
