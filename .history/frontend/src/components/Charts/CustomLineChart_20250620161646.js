import React from 'react';
import { XAxis, YAxis, Tooltip, ResponsiveContainer, CartesianAxis } from 'recharts';

const CustomLineChart = ({ data }) => {
  return (
    <div>
      
    </div>
  )
}

export default CustomLineChart
