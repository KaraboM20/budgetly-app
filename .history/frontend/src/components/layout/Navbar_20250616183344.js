import React, { useState } from 'react';
import { HiOutlineMenu, HiOutlinex } from ''

const Navbar = ({activeMenu}) => {
    const [openSideMenu, setOpenSideMenu] = useState(false);
  return (
    <div>
      
    </div>
  )
}

export default Navbar
