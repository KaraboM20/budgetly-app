import React, { useState } from 'react';
import { HiOutlineMenu, HiOutlinex } from 're'

const Navbar = ({activeMenu}) => {
    const [openSideMenu, setOpenSideMenu] = useState(false);
  return (
    <div>
      
    </div>
  )
}

export default Navbar
