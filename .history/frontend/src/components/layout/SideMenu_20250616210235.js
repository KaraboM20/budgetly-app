import React from 'react';
import { SIDE_MENU_DATA } from '../../utils/data';

const SideMenu = () => {
    const { user, clearUser } = useCon
  return (
    <div>
      
    </div>
  )
}

export default SideMenu
