import React, { useContext } from 'react';
import { SIDE_MENU_DATA } from '../../utils/data';

const SideMenu = () => {
    const { user, clearUser } = useContext(User)
  return (
    <div>
      
    </div>
  )
}

export default SideMenu
