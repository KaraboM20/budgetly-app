import React, { useState } from 'react';
import { HiOutlineMenu, Hi}

const Navbar = ({activeMenu}) => {
    const [openSideMenu, setOpenSideMenu] = useState(false);
  return (
    <div>
      
    </div>
  )
}

export default Navbar
