import React from 'react'

const InfoCard = ({icon={<IoMdCard />}
            label="Total Balance"
            value={addThousandsSeparator(dashboardData?.totalBalance || 0)}}) => {
  return (
    <div>
      
    </div>
  )
}

export default InfoCard
