import React from 'react'

const InfoCard = ({icon, label, value}) => {
  return (
    <div>
      <div>{icon}</div>
      <div>
        <h6>{label}</h6>
        <span?
      </div>
    </div>
  )
}

export default InfoCard
