import React from 'react';
import { LuUtensils, LuTrendingDown, LuTrendingUp, LuTr } from 'react-icons/lu';

const TransactionInfoCard = ({title, icon, date, amount, type, hideDeleteBtn}) => {
  return (
    <div>
      
    </div>
  )
}

export default TransactionInfoCard
